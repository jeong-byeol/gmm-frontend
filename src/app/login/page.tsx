"use client";

export default function LoginPage() {

    return (
        <div>
            <label>이름</label>
            <input type="text" />
            <label>전화번호</label>
            <input type="tel" />
            <button>회원가입</button>
        </div>
    );
}